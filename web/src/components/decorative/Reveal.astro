---
interface Props { as?: "div" | "section" | "article"; delay?: number; class?: string; }
const { as = "div", delay = 0, class: className = "", ...rest } = Astro.props;
const Tag = as;
---
<Tag class={className} data-reveal style={`--reveal-delay:${delay}ms`} {...rest}><slot /></Tag>
